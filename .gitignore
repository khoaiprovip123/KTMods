# rom-kitchen — TOÀN BỘ trên Git (APK + tool qua Git LFS)
# Chỉ bỏ qua build artifacts

cache/
work/
out/

# giữ LFS: *.apk *.jar *.exe *.dll *.so (xem .gitattributes)
# KHÔNG ignore assets/ và tools/ — để all-in-git

# vẫn ignore file rác
*.log
*.tmp
.DS_Store
Thumbs.db
