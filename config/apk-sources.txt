# apk-sources.txt — link tải APK mod (mỗi dòng 1 URL)
# setup.ps1 sẽ tải về assets/apks/
# Google Drive share / link .apk trực tiếp
# Dòng # là ghi chú
#
# Nếu dùng Git LFS (all-in-git) thì file này có thể để trống.
# Nếu không dùng LFS — dán link APK vào đây để CI/máy khác tự tải:
#
# https://drive.google.com/file/d/FILE_ID/view?usp=sharing
# https://example.com/MiuiHome.apk
#
# === Link APK (nếu không push LFS) ===
